# 化学错题智能诊断系统 - 部署指南

## 快速部署（推荐新手）

### 第一步：准备环境

#### 方法A：Windows系统
1. 安装Python 3.9+
   - 访问 https://www.python.org/downloads/
   - 下载Python 3.10或更高版本
   - **重要**：安装时勾选"Add Python to PATH"

2. 打开命令提示符（CMD），验证安装：
```bash
python --version
pip --version
```

#### 方法B：Mac系统
```bash
# 使用Homebrew安装Python
brew install python3

# 验证
python3 --version
```

---

### 第二步：获取AI API Key

#### 方案一：通义千问（推荐国内用户）

1. 访问阿里云DashScope：https://dashscope.console.aliyun.com
2. 注册/登录阿里云账号
3. 开通DashScope服务
4. 创建API Key并保存（只显示一次！）

**免费额度**：每月有免费调用额度

#### 方案二：DeepSeek（性价比高）

1. 访问 https://platform.deepseek.com
2. 注册并登录
3. 创建API Key

**价格**：非常便宜，适合教育场景

#### 方案三：Ollama（完全免费，需要电脑有显卡）

1. 访问 https://ollama.com
2. 下载并安装Ollama
3. 下载模型：
```bash
ollama pull qwen2.5:7b
```

---

### 第三步：安装项目

1. 将项目文件夹放到合适位置（如桌面）

2. 打开终端/命令提示符，进入项目目录：
```bash
cd 路径/chemistry_diagnosis_system
```

3. 安装依赖：
```bash
pip install -r requirements.txt
```

---

### 第四步：配置API Key

#### 方式一：创建.env文件（推荐）

在项目文件夹中创建`.env`文件，内容如下：

**使用通义千问：**
```
AI_PROVIDER=qwen
QWEN_API_KEY=你的API密钥
```

**使用DeepSeek：**
```
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=你的API密钥
```

**使用Ollama（本地）：**
```
AI_PROVIDER=ollama
OLLAMA_URL=http://localhost:11434/v1
OLLAMA_MODEL=qwen2.5:7b
```

#### 方式二：在应用中配置

启动应用后，在左侧菜单选择"系统设置"，在"AI配置"标签页中输入API Key。

---

### 第五步：启动应用

在终端中运行：
```bash
streamlit run app.py
```

等待几秒，浏览器会自动打开，访问 http://localhost:8501

---

## 常见问题

### Q1: 提示"pip不是内部或外部命令"
**解决**：重新安装Python，确保勾选了"Add Python to PATH"

### Q2: 安装依赖时报错
**解决**：
```bash
# 先升级pip
python -m pip install --upgrade pip

# 再安装依赖
pip install -r requirements.txt
```

### Q3: AI服务连接失败
**解决**：
1. 检查API Key是否正确
2. 检查网络连接
3. 确认API Key已激活并有额度

### Q4: 浏览器显示异常
**解决**：使用Chrome或Edge浏览器访问

### Q5: 端口被占用
**解决**：启动时指定其他端口
```bash
streamlit run app.py --server.port 8502
```

---

## 高级配置

### 修改端口
```bash
streamlit run app.py --server.port 8888
```

### 修改默认浏览器
```bash
streamlit run app.py --server.headless true
```

### 开启自动重载（开发模式）
```bash
streamlit run app.py --builder enableSourceCodeEd
```

---

## 数据备份

系统使用SQLite数据库，数据文件位于：
`data/chemistry_diagnosis.db`

定期备份此文件即可。

---

## 卸载/重装

如需重装：
1. 删除项目文件夹
2. 删除数据文件夹（如果不需要历史数据）

---

## 技术支持

如遇到问题，请联系您的学校信息技术教师协助解决。
